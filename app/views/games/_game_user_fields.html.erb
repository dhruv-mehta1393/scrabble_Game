<div class="form-group">
  <%= f.label "player" %>
  <%= f.collection_select(:user_id, User.all, :id, :username, { :prompt => 'Select User'}, { class: 'form-control' }) %>
</div>

<div class="form-group">
	<%= f.label "player score" %>
	<%= f.number_field :score, class:'form-control' %>
</div>
